VITE_API_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000

# Geocoding Configuration
# Choose one provider: 'nominatim' (free, no key), 'locationiq', or 'mapbox'
VITE_GEOCODING_PROVIDER=nominatim

# LocationIQ API Key (optional - only if using locationiq provider)
# Get free API key at: https://locationiq.com/ (5,000 requests/day free tier)
# VITE_LOCATIONIQ_API_KEY=your_locationiq_api_key_here

# Mapbox API Key (optional - only if using mapbox provider)
# Get free API key at: https://www.mapbox.com/ (100,000 requests/month free tier)
# VITE_MAPBOX_API_KEY=your_mapbox_api_key_here
